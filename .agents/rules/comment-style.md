# Comment Style

- Always use `/* */` block comments, never `//` line comments.
- Keep comments plain. No decorative borders like `======` or `------` or `******`.

```js
/* good */
/* bad: // never use this */
/* bad: ====== never use this ====== */
```
